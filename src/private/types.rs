/// The type for indexing into vertices when drawing shapes.
pub type Idx = u16;

// TODO: before adding any changes to this, make sure that shaders
// are changed as well.
/// The floating point type used for positions and colors.
pub type Float = f32;
